
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access for features like server-side AI analysis,
    // but restrict writes to authenticated users to protect data integrity.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Allow public creation of enrollment requests from the public form
    match /enrollment_requests/{requestId} {
      allow create: if true;
    }
  }
}
