
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default to requiring authentication for all read/write operations.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
